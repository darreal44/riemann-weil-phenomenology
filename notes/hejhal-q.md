# Q via Hejhal — what actually changes

Hejhal 1976 §§6.7, 10.2 is the
Selberg trace on the *surface*:
ψ(1/2+ir)+ψ(1+ir) against h(r).
That is not L(s,f) of a Maass
newform.

The L-function formula
(Booker / standard Weil for
Λ(s)=π^{-s} Γ((s+iR)/2)Γ((s−iR)/2) L)

    arch(t) = Re[ ψ(1/4 + i(R+t)/2)
                + ψ(1/4 + i(−R+t)/2) ]

plus −log π. Our D₂(s₀),
s₀=1/4+iR/2, is the *y-space*
kernel whose Fourier transform
is supposed to be that ψ, by
the same Laplace identity that
works for real s₀ (Dirichlet).

The missing check: on the
constant mode,

    A₀₀[D₂]  ?=?  ∫ hat₀₀(t) arch(t) dt

If they disagree, D₂(s₀ complex)
is not that arch and Q is the
wrong operator. If they agree,
INDEF is a window/R effect and
Hejhal will not save μ=8.

Do that comparison before
another μ. Do not paste
Hejhal’s K_{iR} algorithm
(that computes a_n, which
we already have from Zenodo).
