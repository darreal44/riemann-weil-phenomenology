# Maass Γ pair

Textbook even
Maass on Re s=1/2:

    Γ_ℝ(s+iR) Γ_ℝ(s−iR)
    s0 = 1/4 ± i R/2

Odd:

    s0 = 3/4 ± i R/2

That pair was
already in
scan_q_maass.py.
INDEF at μ=22,38
is not a wrong
shift of 1/4.

What changed:
CST copied from
GL2_FIX (cut per
panel, (1/2)log N
− log π − γ).
symmetry in the
Zenodo JSON
selects 1/4 vs
3/4. MAASS_PAIR=half
is an experiment
only.

Adding two
conjugate
Dirichlet panels
*is* the explicit
formula (kernels
add). It is not
2×A_even except
at R=0.

Do not grow μ
on the same
INDEF curve and
call it a Γ fix.
