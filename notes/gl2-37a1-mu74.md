# 37a1 drop 3 at μ=74 and μ=80: still out, plateau

Preregistered (`report/prereg-37a1-mu74.md`), after the μ=62
KILL: linear in μ from +0.38 (μ=38) to +0.093 (μ=62) crosses
at μ≈70, so drop 3 → negative at μ=74 (and at μ=80).
Prime-side Q. Not the Gram. Not RH.

## Execution

`gl2_quorum_scan.py 37a1 74 80 50` (22 processes) and
`… 37a1 80 80 50 --drops 3` (2 processes), 223 s wall.

| μ | full λ₀ | full ℓ | drop 3 λ₀ |
|---|---|---|---|
| 38 | — | 9.0 | +0.38 |
| 62 | 5.258×10⁻⁷ | 14.46 | +0.093 |
| **74** | 4.967×10⁻⁸ | 16.82 | **+0.090** |
| **80** | 2.847×10⁻⁸ | 17.37 | **+0.090** |

At μ=74 every other interior voting prime is still necessary
(2, 5, 7, …, 61). 17 and 19 remain mute (a_p=0, p²>μ). 71 and
73 looked mute only because ppts skipped p>67.

## Verdict: KILL

3 does not join at μ=74. It does not join at μ=80. The linear
threshold μ≈70 is dead. Drop-3 λ₀ has stalled at ≈0.09 while
the full well keeps deepening (ℓ 14.5 → 16.8 → 17.4). Heaviest
last is still the pattern; “it yields once ℓ≳16” is not.

Those two json files used `ppts_of` capped at 67, so 71 and
73 did not enter the sum (drop-71 = full at μ=74). Recomputed
with every p^k: drop-3 stays +0.090 / +0.089; the full well
deepens a little (`notes/gl2-37a1-drop3-hi.md`). The plateau
is not that bug. 3 joins on 100..250.

Tables: `report/gl2-37a1-mu74-quorum.json`,
`report/gl2-37a1-mu80-quorum.json`. Judge:
`tests/test_gl2_37a1_mu74.py`.

## Status

| Claim | Status |
|---|---|
| μ=62, drop 3 → negative | false (previous KILL) |
| μ=74, drop 3 → negative | **false** (KILL) |
| μ=80, drop 3 → negative | **false** (KILL) |
| drop-3 λ₀ plateau ≈ 0.09 on 62–80 | judged |
| (∀ L) Q_L ≥ 0 | RH; not this note |
