# Taylor remainders of q_i

q_i
matches
g, g'
at
the
left
end
and
uses
m_i ≤ g''.
The
pointwise
remainder
is
exactly

    g(y)−q_i(y)
      = ∫_{y_i}^y (y−t) (g''(t)−m_i) dt
      ≤ (leftover_i / 2) (y−y_i)²
      ≤ leftover_i h_i² / 2.

On
a
well
slab
h≈0.102,
leftover≈1.3,
the
cap
is
1.3×0.010/2≈0.0066
pointwise
at
the
right
end.
Integrated
against
½w
on
that
slab
this
is
a
few
10⁻⁴.
Eight
slabs
sum
to
the
observed
0.0028
once
the
weight
and
the
average
(not
the
endpoint
cap)
are
in.

A
full
Taylor
of
g
at
y_i
to
order
2
with
remainder
g'''(ξ) t³/6
is
a
different
object
(the
cubic
bound).
Here
the
order-2
jet
is
exact
and
the
remainder
is
carried
by
g''−m,
not
by
g'''.
g'''
only
explains
how
fast
the
leftover
grows
across
the
slab
(~1.3
over
0.10
≈ g'''×h).

Not
Weil.
