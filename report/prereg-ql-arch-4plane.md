# Preregistration: CST 4-plane vs 2 m_Q(ω_n) at μ=5, χ₃

Locked before the run. Archimedean
identification on the same 4-plane
as the H₄ sign. Not Fourier of θ
(that mass is not 2). Diagonals:
Q_nn^{arch} vs 2 m_Q(ω_n), ω_n=2π n/L,
m_Q = C_A_lo/2 + k(t), w₂=0.
Hybrid = 2 m_Q on the diagonal, CST
off-diagonal, plus Q_pk primes.
Not RH.

**Prediction.** CST+p^k λ_min(H₄)>0
(matches #69, ~10⁻⁵). Hybrid λ_min(H₄)<0
(gap on n=0 is the known 0.62). The
p^k rescue of the sign is CST-specific.
2 m_Q(ω_n) < Q_nn^{arch} for n=0,1,2,3.

**Kill.** Hybrid λ_min≥0, or CST+p^k
λ_min≤0.
