# Regularity of r

D₂(y)
= 2 e^{−2 s₀ y} / (1−e^{−2y}).
Let
u=2y.
Then
1/(1−e^{−u})
= e^{u}/(e^{u}−1)
has
a
Laurent
series
1/u + 1/2 + ∑ B_{2k} u^{2k−1}/(2k)!
(Bernoulli).
Times
e^{−s₀ u}
= 1 − s₀ u + O(u²)
gives

    D₂(y)
      = 1/y
        + (1−2 s₀)
        + analytic
        at
        0.

For
s₀=3/4,
1−2s₀=−1/2,
so
r(y)
= D₂−1/y+1/2
is
analytic
at
0
(the
1/y
and
the
constant
have
been
removed).
On
(0,L]
every
factor
is
C^∞
and
1−e^{−2y}≠0.
Hence
r
is
C^∞
on
[0,L].
‖r^{(n)}‖_∞
is
finite
for
every
n.
IPP
to
any
order
is
legal.
The
first
derivative
is
all
S_k
needs.

Not
RH.
