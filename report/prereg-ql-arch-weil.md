# Preregistration: classical Γ pairing on W_L

Locked before the run. Archimedean
identification named in
`Qnm-is-not-Weil.md` and by the
10^{-5} of Q_pk H₄: that sliver is
neither Weil-positive nor
Weil-negative until the archimedean
piece is the classical Γ term.
Not Fourier of θ. Not RH.

On W_L, θ(y)=0 for y>L. Two writings
of the same pairing, and one mix:

1. CST+Gauss (Q_nm arch):
   (F₀/2) CST + ½ ∫_0^L D₂(F₀ EC−θ),
   CST = log(q/π)−γ−log(1−e^{−2L}).
2. Mixed: (1) plus (ψ(s₀)+γ) I.
   This replaces −γ by ψ(s₀) while
   keeping the EC integrand. Illegal.
3. Digamma: (F₀/2) C_A_lo
   + ½ ∫_0^L D₂(F₀−θ),
   C_A_lo = log(q/π)+ψ(s₀)+∫_L^∞ D₂.

Identities used, not fitted:
Frullani ∫_L^∞ D₂ EC = −log(1−e^{−2L});
Weierstrass ∫_0^∞ D₂(EC−1) = ψ(s₀)+γ.
Hence (1)=(3) on every matrix element.
C_A_lo is the constant of (3), not
the matrix element (#69 compared
them). 2 m_Q(ω_n) is a plane wave,
not Q(φ_n) (#70).

χ₃ and χ₅, μ=5, 4-plane, plus Q_pk.
Not (∀ L). Not the class.

**Prediction.** Frullani and
Weierstrass hold. max |Arch_CST −
Arch_digamma| < 10^{-6} on both
4-planes. Mixed shifts every
diagonal by ψ(s₀)+γ
(χ₃: ≈ −0.509; χ₅: ≈ −3.650).
λ_min(Q_pk H₄) under (1) and under
(3) stay +1.4×10^{-5} on χ₃.
λ_min mixed < 0. The 10^{-5} is
still not Weil-positive (Courant
section) and not Weil-negative
(the identified 4-plane is >0).

**Kill.** |Arch_CST − Arch_digamma|
> 10^{-6} on the χ₃ μ=5 4-plane,
or Frullani fails, or the
Weierstrass integral misses ψ+γ
by more than 10^{-6}.
