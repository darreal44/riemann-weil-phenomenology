# ξ track

Seventh cell: the trivial character, q=1, a=0, plus s(s−1).

At s=½ the two poles cancel in the value of ξ'/ξ:

    1/(½) + 1/(−½) = 0

so the archimedean symbol at t=0 is exactly the even formula at q=1:

    ½ ψ(1/4) − ½ log π = −2.686

CST of Q_nm at q=1, L=log 3:

    log(1/π) − γ − log(1−e^{−2L}) = −1.604

Gap +1.08, same order as χ₅/χ₈ (+1.9), not the χ₃ 0.06. The trivial
character sits with the even cells for the constant, and CST does not
match.

What a ξ track would compute, in order:

1. A₀₀
   for q=1 at L=log 3 (arch only) versus −2.686 and versus C_A^{lo} of
   m_Q for ζ.
2. Schur
   on W_{log 3} with that A and T₂ (ζ(−1) wait: χ=1, χ(2)=+1,
   w₂=+0.490). That is the χ₁₇ cell with q=1.
3. Do
   not harvest γ first. Prime side is already Q_pk with χ≡1.

Li coefficients are a different ξ track (moments on a circle), already
in the journal. This one is the same Schur machine on the missing q=1
even +1 cell.

Not run. Not RH.
