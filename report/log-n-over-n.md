# The O((log n)/n) bound

∑_{k≥1} 1/(k(2n+k))
= (1/(2n)) ∑ (1/k − 1/(k+2n))
= (H_{2n} + o(1) − (H_∞−H_{2n}))
/ (2n)
= (log n)/n
+ O(1/n).

At
n=32
the
sum
is
0.074.
Times
2 S_∞ / π
with
S_∞≈π/2
gives
2·(π/2)/π · 0.074
= 0.074
as
the
model
size
of
∑ |½ ∫ D₂ θ|
if
n S_n−m S_m
= k S_∞
exactly
and
w₂=0.

Measured
partial
row
0.67
over
15
terms
is
larger
because
it
still
includes
the
near-far
k=1,2,…
at
full
strength
and
w₂ θ(log 2).
The
O((log n)/n)
is
the
*tail
of
the
tail*.
The
first
ten
k
must
stay
either
computed
or
bounded
by
the
true
|Q|
(0.29, …),
not
by
½π.

So
the
bound
is
legal
for
k≥K
and
useless
as
a
single
blanket
from
k=1.
That
is
the
same
near/far
split
already
in
ρ_N / ρ_far.
The
gain
is
only
in
ρ_far.

Not
RH.
