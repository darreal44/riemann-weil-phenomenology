# Preregistration: I_{[0,1]} tangent/floor switch

Locked before the run. Same witness
v=(4,−3,1)/√26, χ₅ μ=16. Not the
chord of θ (miss 0.22). Not the
parent 3-piece that keeps the
tangent until y_min (miss 0.224).

g = 2 e^{−3y/2} − θ_v, elementary.
g'(0)<0, unique min g_min at y_min,
inflection y_inf. Convex ⇒ every
tangent lies below g. The tangent
at 0 is g'(0) y. The tangent at
the min is the floor g_min. They
meet at y_sw = g_min / g'(0).

Comparison g_lo:
  [0, y_sw]     tangent at 0
  [y_sw, y_inf] floor g_min
  [y_inf, 1]    chord of g

a_lo = ½ w g_lo. Integrate a_lo
(the comparison), not a itself.
True I = ∫ a on [0,1], same shipped
a. Window ±0.003 on that integral
(the A-room that keeps Q(v)>0).
Not RH. Finite μ, one v.

**Prediction.** y_sw < y_min. The
signed gap I_true − I_lo is in
(0.05, 0.15): tighter than the
parent 0.22, still outside ±0.003.
Q_lo from I_lo is negative. This
comparison does not close.

**Kill.** Gap < 0.003 (would have
closed), or gap ≥ 0.20 (same as
the parent 3-piece / chord, not a
new comparison), or y_sw ≥ y_min.
