# Digamma in this form

    ψ(s) = Γ'(s)/Γ(s)

Weierstrass:
ψ(s) = −γ −1/s + ∑_{k≥1}
[1/k − 1/(k+s)].

#71
uses
that
to
identify
−γ + E_C
with
ψ(s₀)
plus
bare
D₂
on
W_L.
The
ladder
∑ e^{−2(s₀+k)y}
is
the
same
sum
in
the
delay
variable.

Values
already
used:

    ψ(1/4) = −γ −π/2 −3 log 2
    ψ(3/4) = −γ +π/2 −3 log 2
    ψ(1)   = −γ
    ψ(1/2) = −γ −2 log 2

Reflection:
ψ(1−s)−ψ(s) = π cot(π s).
That
is
the
functional
equation
of
Γ
in
logarithmic
form,
and
the
reason
s₀
and
1−s₀
are
a
pair
(1/4
and
3/4).

Recurrence:
ψ(s+1)=ψ(s)+1/s.
Each
+1
in
the
pole
ladder
is
one
term
in
D₂.

The
“digamma
writing”
of
A
(#71)
is
(F₀/2) C_A^{lo}
+ ½∫ D₂(F₀−θ).
It
agrees
with
CST+Gauss
to
10⁻¹⁵
on
the
4-planes.
C_A^{lo}
is
the
constant
of
that
writing,
not
A(φ₀).

No
new
ψ
identity
is
needed
for
the
takes.
The
open
cap
lemma
does
not
use
ψ.
