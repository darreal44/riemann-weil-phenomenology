# Operator norms, now

    ‖Θ(y)‖
      ≤ √2
      on
      the
      mid-band
      (#89,
      one
      L).
      Used
      as
      1.0
      in
      theta_op_bound
      in
      the
      current
      tree
      (a
      lab
      cap,
      check
      the
      function
      before
      quoting
      √2
      there).
    ‖H_Hankel‖
      = π
      on
      ℓ²(ℕ)
      and
      on
      every
      tail.
      ½π
      in
      Off_far.
    ‖A_near‖₂
      = ρ_N
      computed
      (~0.29
      on
      χ₃).
    ‖Q̂_L‖
      exists
      (bounded
      form
      on
      W_L)
      and
      is
      not
      needed
      for
      the
      sign
      of
      λ_min.

The
norm
that
blocks
χ₃
is
the
second
one.
Shrinking
it
on
the
tail
is
open.
The
first
one
is
already
better
than
2.
Do
not
fuse
them.

Not
RH.
