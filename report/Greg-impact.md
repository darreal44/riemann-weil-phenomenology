# Impact of G_reg

G_reg=D₂−1/y is
the continuous
even kernel
after the 1/y
singularity is
peeled off
(`kernel-regularisation.md`).

    s₀=1/4   G_reg(0⁺)=+½
             decreasing, concave
             χ₅, χ₈
    s₀=3/4   G_reg(0⁺)=−½
             increasing, convex
             χ₃, χ₄

On W_L the
pairing still
includes
∫g/y and CST,
so a negative
G_reg is not
Q<0. It *is*
a smaller H:
λ_H(χ₃)=0.018
vs 0.053 (χ₅),
0.070 (χ₄ at
h=20), 0.257
(χ₈).

That is the
impact that
matters for
Schur. The
sliver S_diag
=0.008 on χ₃
is G_reg
eating H
before T₂
and ρ_far
arrive. χ₄
has the same
G_reg and no
atom: H
survives
(#60). χ₃
does not
have that
margin.

Pólya: neither
sign of G_reg
is convex and
decreasing to
0. Textbook
Pólya is
closed for
both parities
(`Greg-convexity.md`).
Krein does
not care
about the
sign of
G_reg as a
function,
only PD of
the full
kernel
(G_reg + CST
+ atom).

Bochner on
ℝ already
failed on
C_A^{lo}<0
for every χ.
G_reg’s sign
is not that
failure.
