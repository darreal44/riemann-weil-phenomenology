# Preregistration: g_lo at s₀=3/4 (odd χ)

Locked before the run. Same v=(4,−3,1)/√26,
μ=16, L=log 16. Not another v on χ₅
(`av-I01-vscan.md`). The character enters
A only through the Bose weight s₀:
odd χ₃ (s₀=3/4) has

    w = 2 e^{−3y/2}/(1−e^{−2y})
    g = 2 e^{−y/2} − θ_v

θ_v is the same mix. If the convex
well on (0,1) dies, g_lo is an even /
χ₅ artefact. Not Weil. Not RH.

N=4 well+rise + chord if a unique
min and an inflection exist on (0,1)
with g''>0 then <0. Same scheme as
vscan, not leftover-adaptive.

**Prediction.** g'(0)<0 and g''(0)>0,
so a well still exists (θ'' is the
same, only the Bose 4.5 vs 0.5
changes). Unique ymin, yinf in (0,1).
The N=4 gap need not sit in ±0.003.
The well is not a χ₅ artefact.

**Kill.** No convex well+rise on (0,1)
(g_lo is then an even artefact), or
g_lo not ≤ g when a well is claimed.
