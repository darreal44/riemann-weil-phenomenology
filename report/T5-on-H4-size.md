# Size of T₅ on H₄ just after 5

χ₃(5)=−1. At μ=5.1, θ₀₀(log 5) =0.024, |w₅|=0.720, ΔH₀₀ = −w₅ χ(5) θ₀₀ =
+0.017.

A push *up* on the constant mode, O(10⁻²), after the crossing already
went negative by 1.6×10⁻⁴ without T₅.

So if anything, T₅ on χ₃ tries to help H₀₀ once p=5 is interior. It does
not create μ*. It does not cancel a −10⁻⁴ that already exists in H₄ at
μ=5 (the well is the 2×2 / 4×4 mix, not only H₀₀).

Same conclusion as `T5-on-H4.md`, with the first number.
