# Cheeger

h(G) ≤ √(2λ₂) (or λ₂ ≤ 2h depending on normalisation): a spectral gap
controls a conductance cut. On an interval the same idea is the
classical isoperimetric profile of [0,L/2], which is trivial (the cut is
a point).

#89 is not a Cheeger estimate. The √2 cap is CS on three explicit pairs,
not a gap of a Laplacian. There is no λ₂ of a graph to feed into Cheeger
and get θ.

Not a campaign.
