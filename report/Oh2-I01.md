# O(h²) on [0,1]

Pointwise
remainder
≤ leftover h² / 2
with
leftover = O(h)
if
g'''
is
bounded
(leftover
≈ |g'''| h).
Then
pointwise
O(h³)
and
the
integral
against
a
bounded
weight
on
a
fixed
interval
is
O(h³)×#slabs
= O(h²)
because
#slabs
= O(1/h).

Check
against
the
runs
(well+rise
width
≈0.77,
h=0.77/N_eff):

    N_well   gap
    1        0.047
    2        0.024
    4        0.0053   (well only)
    4+rise   0.00315
    +tail    0.00279

From
N=1
to
N=2
a
factor
2
(not
yet
in
the
O(h²)
regime:
one
slab
uses
a
global
inf).
From
N=2
to
N=4
a
factor
~4.5
on
the
well,
compatible
with
h²
once
the
mesh
resolves
g'''.
N=8
on
well+rise
should
cut
the
0.0028
by
about
4
if
the
law
has
set
in
→ gap
~7×10⁻⁴,
comfortably
inside
±0.003
and
still
one
v.
Executed
(`notes/av-I01-n8.md`):
gap
0.000405,
cap
ratio
0.123.

That
is
a
rate,
not
Weil.
