# 3-atom least squares for G_reg on [0,L]

    G_reg(y) ≈ c₀ + ∑_{j=1}^3 m_j cos(t_j y)
    m_j free, then read the sign.

Even s₀=1/4:

    t = 0 (in c₀), 2.03, 8.10, 13.67
    masses 0.431, 0.064, 0.002, 0.0007
    all ≥ 0
    rms = 2.9×10⁻⁴  max = 1.0×10⁻³

G_reg even on [0,L]
is matched by a
positive atomic μ.
That is a numerical
Krein extension of
the *continuous even
part*. Symbol of
this extension is
the masses, hence
≥0. Bochner would
take G_reg even
alone.

Odd s₀=3/4:

    one mass −0.70
    rms = 4×10⁻⁴
    no positive
    3-atom fit.

The y-atom ±log 2
is not in this
fit. For χ₅ it
subtracts w₂ cos
in *t* from a
positive symbol
and can recreate
the dip of #57.
For χ₈, w₂=0:
this 3-atom μ
is already a
candidate
certificate of
G_reg, hence of
Q on W_L if one
accepts rms 10⁻³
as a match.
That is the
first constructive
hint. Not a proof
(grid of t, 3
atoms, G_reg not
full G).

Next honest
step: lock χ₈
(no atom), refine
the atomic μ,
compare ˆμ to
G_reg at more
y, then decide
whether the
residual can be
eaten by a 4th
atom still ≥0.
