# Impact of w₂

    w₂ = χ(2) log 2 / √2
    |w₂| = 0.490129
    χ(2) ∈ {−1,0,+1}
    On these four χ,
    never +1.

Linear in every
matrix element
that sees the
lag:

    ΔH_nm = −w₂ θ_nm(log 2)

Known:

    ΔH₀₀ = −w₂ · 0.738 = +0.362   (χ=−1)
    ΔH₀₁ = −w₂ · 0.330 = +0.162
    ‖T₂‖ ≤ |w₂| · 1     = 0.490   (#59)

If w₂ were 0,
χ₅ would look
like χ₈’s
parity with
q=5 (H smaller
than χ₈ because
log(q/π)+ψ is
more negative).
χ₃ would look
like χ₄ with
q=3. #60 took
χ₄. The bit
χ(2) is the
whole arithmetic
difference
inside this
window (only
p=2 is present).

w₂ does not
change G_reg
or s₀. It does
not change
Hankel. It
only writes
the rank-one
lag into H
and into T.
Sign(χ(2))
decides
whether that
lag helps the
diagonal and
hurts det, or
is absent.

No continuous
knob: w₂ is
fixed by the
character.
Turning it
down is
dropping T₂,
which is
another
operator
(drop-p),
already
measured
elsewhere
(37a1 / 3).
