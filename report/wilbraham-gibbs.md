# Wilbraham–Gibbs constant

    G
      = (1/π) ∫_0^π sin t / t dt
      = Si(π)/π
      ≈ 0.58949
    overshoot
      = G − 1/2
      ≈ 0.08949
      (~9 %
      of
      the
      jump).

It
quantifies
the
cutoff
already
named.
It
does
not
enter
S_k,
θ_nm,
or
S_lo.
No
need
to
tabulate
Si(π)
further.

Not
a
campaign.
