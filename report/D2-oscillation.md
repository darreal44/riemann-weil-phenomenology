# Oscillation: D₂ is not the oscillator

D₂(y,s₀)
=
2 e^{−2 s₀ y}/(1−e^{−2y})
is
positive
and
decreasing
on
(0,L].
It
does
not
change
sign.
The
oscillation
is
θ_nm(y)
(sines
at
ω_n=2π n/L).

∫ D₂ θ_nm
is
a
Fourier
coefficient
of
D₂
(times
the
closed
form
of
θ).
Riemann–Lebesgue:
the
coefficient
→0
as
n or m→∞.
Rate:
D₂
is
C^∞
on
(0,L]
and
~1/y
at
0,
so
the
Fourier
cosine
coeffs
on
[0,L]
decay
like
the
coeffs
of
a
function
with
an
integrable
singularity,
typically
O((log n)/n)
or
O(1/n)
after
one
integration
by
parts
on
[δ,L]
plus
an
O(δ)
end.

That
rate,
once
written
with
an
explicit
constant
for
s₀=3/4,
is
the
missing
C
of
`polya-constant.md`.
It
replaces
0.82/k
by
something
summable
in
k
(or
already
O((log n)/n)
per
row).

D₂
itself
has
no
useful
oscillation
to
analyse.

Not
RH.
