# Preregistration: I_{[0,1]} by hand — Gauss table and 3-piece g

Locked before the run. Rational witness
v=(4,−3,1)/√26, χ₅ μ=16. The chord of
θ misses I_{[0,1]} by 0.22 the wrong
way (`A-v-01.md`). This campaign
evaluates two hand objects:

1. 3-point Gauss of a(y)=½ w(2e^{−3y/2}−θ_v)
   as a table of elementary θ_nm at the
   three nodes (origin’s remaining
   arithmetic check).
2. A 3-piece comparison of g=2e^{−3y/2}−θ_v:
   tangent at 0 on [0,y_min], floor g_min
   on [y_min, y_inf], chord on [y_inf, 1],
   with y_min the unique root of g'=0 and
   y_inf the unique root of g''=0 on (0,1).
   Convex g ⇒ g ≥ tangent; at the min,
   g ≥ g_min; on the concave tail, g ≥ chord.

Not a covering lemma. Not RH.

**Prediction.** Gauss3 agrees with
−0.70066 to 10^{-4}. The 3-piece lower
bound misses I_{[0,1]} by more than 0.15
(same size as the chord, opposite
direction) and does not close the
±0.003 A-window. Q_lo from that bound
is negative.

**Kill.** Gauss3 disagrees with −0.70066
by more than 10^{-3}, or the 3-piece
gap is < 0.05 (the comparison would
have closed).
