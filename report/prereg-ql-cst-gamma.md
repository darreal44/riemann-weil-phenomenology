# Preregistration: CST+Gauss vs C_A_lo vs ψ(s₀)

Locked before the run. Archimedean
identification named in
`Qnm-is-not-Weil.md`. Constant mode
only, χ₃ and χ₅, L=log 3 and log 5.
No zeros. Not RH.

C_A_lo = log(q/π)+ψ(s₀)+∑ e^{-2(s₀+k)L}/(s₀+k)
is the Bochner constant (`ql_operator_bound`).
Q_nm CST = log(q/π)−γ−log(1−e^{-2L}), plus
Gauss of D₂ against θ. If they were the
same pairing on φ₀, arch_00 := Q_nm(0,0)
with w₂=0 would equal C_A_lo.

**Prediction.** arch_00 ≠ C_A_lo. Gap is
O(1) on χ₃ and χ₅. C_A_lo is closer to
log(q/π)+ψ(s₀) than arch_00 is. CST+Gauss
is not the classical Γ constant on φ₀.

**Kill.** |arch_00 − C_A_lo|<10^{-3} on χ₃
at L=log 3.
