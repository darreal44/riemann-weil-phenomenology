# Young on D₂, L=log 3

∫_0^L D₂(y) dy ≈ 28
(s₀=1/4) and ≈ 27
(s₀=3/4). CST is
O(1). A crude Young

    |½ ∫ D₂ (F₀ e^{−c y} − θ)|
        ≤ ½ ∫ D₂ · 4
        = 2 ‖D₂‖₁
        ≈ 56

or even CST − ‖D₂‖₁
≈ −28, is two orders
below the scale of
the ladder (0.04)
and of λ_min(A)
(±0.2). The bound
does not see
g(0)=0, which
cancels the 1/y of
D₂ at the origin
and is the only
reason A is O(1)
rather than O(30).

Young without the
cancellation is
dead. Bernstein
versus L² is the
same mistake at
the lag. The
operator bound
has to keep
F₀ e^{−c y} − θ(y)
as one kernel,
not split them.

Tried. Failed.
Not the step.
