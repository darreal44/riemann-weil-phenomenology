# T2 oscillation

T2 = −w2 θ(log 2), w2(χ3)=−0.490.
Phase: n log 2 / log 3 mod 1, α≈0.63093
(period 1/α≈1.585 hats). Dense, sign flips
often.

On χ3, n=30..45:
    θ_nn ∈ [−0.74,+0.73]
    T2 on diagonal ∈ [−0.36,+0.36]
    Trough Q_34=4.192 when θ≈−0.71
    Peak  Q_38=5.007 when θ≈+0.73

qmin_far is that trough, not the trend
(Q_32=4.607). Amplitude ~|w2|·1 (θ_nn
bounded by ~1) so ~0.49 peak-to-peak
half-swing 0.36.

B1 weights Q_{n,n+1} ride the same phase
(std 0.20, max 0.29). Already bounded
(||B1^Θ||≤4|w2|/π). The new cost of the
oscillation is the trough that prices
ρ_far.

Not a new period to harvest. Not RH.
