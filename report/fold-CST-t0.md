# Fold C_A^{lo} into the t=0 atom (χ₈)

5-atom G_reg:
m(0)=0.424 > 0.

#57:
    C_A^{lo}(χ₈)=−0.929
    m_Q(0)=C_A^{lo}/2=−0.464

Fold:

    m(0) + C_A^{lo}/2 = 0.424 − 0.464
                      = −0.041 < 0

The PD atomic
picture of G_reg
dies as soon as
the archimedean
constant sits
on t=0. That is
why Bochner on
the line fails
(α_line=C_A^{lo}<0)
while G_reg
alone looked
extendable.

Schur (#58)
does not put
C_A^{lo} on a
single atom. It
splits H (2×2
cosine) / T / C
and still has
β=+0.055. The
constant is
shared across
modes. Folding
into t=0 is the
wrong packaging
for a
certificate,
even though it
is the honest
Bochner
packaging.

Krein on the
cutoff
restriction
must keep C_A
inside the
interval form,
not as a
negative
δ_0 in t.
