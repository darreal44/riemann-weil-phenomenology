# Quadratic mesh on [0, y_min]

Pick
nodes
0=y₀<y₁<…<y_N=y_min.
On
each
I_i=[y_i, y_{i+1}]
set
m_i = inf_{I_i} g''
(elementary
g_pp,
same
grid
as
#83).
The
piece

    q_i(y)
      = g(y_i)+g'(y_i)(y−y_i)
        +(m_i/2)(y−y_i)²

satisfies
q_i≤g
on
I_i
because
g''≥m_i
and
q_i
matches
jet
1
at
y_i.
At
y_{i+1}
q_i
generally
lies
below
g
(the
mesh
does
not
require
C¹
across
nodes).
g_lo
is
the
broken
parabola
plus
the
old
floor / t_inf / chord
after
y_min.

Uniform
mesh:
h=y_min/N.
N=1
is
#83.
N=4
is
the
h≲0.1
count
of
`quadratic-error.md`.
An
adaptive
mesh
would
put
smaller
h
near
0
where
g''
falls
fastest
(g'''<0).

The
construction
is
legal
as
soon
as
each
m_i
is
a
true
inf
(grid
+
a
bound
on
g'''
to
control
the
missed
dip,
or
a
finer
grid
than
the
target
gap).
That
last
ε
is
the
only
machine
step
left
in
the
hand
story.

Not
Weil.
