# Preregistration: χ₃ Neumann at μ=5.1, T₅ arrival

Locked before the run. Identified Q_pk,
cap √2. μ=5 vs 5.1, h=16 and 24, plus
ρ_far at N_NEAR=32. T infinite. Not
Galerkin. Not RH.

At 5.1, n=5 is interior so t_atoms
already includes |w₅|, but θ(log 5)
sits at the edge. #76: take at 5,
dead at 5.5. This is the missing
point.

**Prediction.** ns(5.1)=[2,3,4,5].
t_atoms(5.1)>t_atoms(5). S_lo(16)>0
at μ=5 (regression). The sign of
S_lo at 5.1 is not predicted: either
the take dies at arrival or it
survives the edge. Not Weil-positive.

**Kill.** ns wrong, or t_atoms does
not jump, or S_lo(16)≤0 at μ=5.
