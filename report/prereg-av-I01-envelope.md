# Preregistration: I_{[0,1]} three-tangent envelope

Locked before the run. Same witness
v=(4,−3,1)/√26, χ₅ μ=16. Not the
chord of θ (miss 0.22). Not the
parent 3-piece (miss 0.224). Not
the tangent/floor switch (miss
0.088). Not Weil. Not RH.

g = 2 e^{−3y/2} − θ_v, elementary.
Convex on [0, y_inf] ⇒ every
tangent lies below g. Three
supporting lines:

  t0   = g'(0) y
  tmin = g_min
  tinf = g(y_inf) + g'(y_inf)(y − y_inf)

They meet at y_sw = g_min/g'(0)
and at y_meet defined by
tmin = tinf. Then the concave
tail is the chord of g.

Comparison g_lo:
  [0, y_sw]       t0
  [y_sw, y_meet]  floor g_min
  [y_meet, y_inf] tinf
  [y_inf, 1]      chord of g

a_lo = ½ w g_lo. Integrate a_lo,
not a. True I = ∫ a on [0,1],
same shipped a. Window ±0.003.
Finite μ, one v. Not (∀ L) Q_L≥0.

**Prediction.** y_min < y_meet < y_inf.
The signed gap I_true − I_lo is in
(0.02, 0.08): tighter than the
switch 0.088, still outside ±0.003.
Q_lo from I_lo is negative. This
comparison does not close. It is
not Weil positivity.

**Kill.** Gap < 0.003 (would have
closed), or gap ≥ 0.088 (not
tighter than the switch), or
y_meet not in (y_min, y_inf).
