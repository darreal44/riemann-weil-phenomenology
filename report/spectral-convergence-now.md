# Spectral convergence, now

Two spectra must not be fused.

    Galerkin λ_min(H_n) ↓ λ_min(Q̂_L) from above (Courant). Rate
    unknown; not a take.
    Neumann S_lo ↑ a number ≤ λ_min(Q̂_L) from below, only if ρ<1. This
    is the windowed bound that replaced Bochner.

I_{[0,1]} on one v is neither. #89 is a bound on Θ(y), not on spec(Q̂).

Weyl-type counting for spec(Q̂_L) is not written and is not needed for
the sign of λ_min. A Weyl law would describe how many eigenvalues sit
above a threshold, not whether the smallest is positive.

Not RH.
