# O(h⁴) versus what we have

Per
slab,
∫_0^h (leftover/2) t² dt
= leftover h³ / 6.
If
leftover
is
O(1)
(m_i
lags
g''
by
a
fixed
amount),
each
slab
is
O(h³)
and
N=O(1/h)
slabs
make
O(h²)
on
[0,1].
That
is
the
N=1–4
regime
(leftover
still
~1).

If
leftover
= |g'''| h
= O(h),
each
slab
is
O(h⁴)
and
the
sum
is
O(h³).
Nobody
gets
O(h⁴)
on
the
whole
interval
from
this
mesh:
that
would
need
leftover
= O(h²)
(matching
g''
at
both
ends,
i.e.
Hermite
or
a
cubic).

Hermite
unsigned
is
O(h⁴)
*per
slab*
and
O(h³)
global
— the
same
global
order
as
the
resolved
mesh,
with
the
wrong
sign
half
the
time.

So
O(h⁴)
is
a
per-slab
count
when
the
mesh
resolves
g''',
not
a
better
global
rate
than
O(h³),
and
not
available
as
a
legal
under-estimator
without
a
safe
m_i.
N=8
is
still
the
O(h²)→O(h³)
transition,
not
an
h⁴
jump.
