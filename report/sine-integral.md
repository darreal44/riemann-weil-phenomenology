# Sine integral

Si(x)
= ∫_0^x (sin t)/t dt,
Si(∞)=π/2,
Si(2π k)
is
already
within
10^{−3}
of
π/2
for
k≥2
(the
alternating
tail
~ cos(x)/x).

In
this
lab
Si(ω_k L)=Si(2π k)
is
the
principal
part
of
S_k.
It
does
not
need
a
new
table.
The
error
S_k − π/2
is
∫ r sin
(`R-term.md`),
not
π/2 − Si(2π k).

Asymptotic:
Si(x)
= π/2 − cos(x)/x
+ O(1/x²).
At
x=2π k,
cos(2π k)=1,
so
π/2 − Si(2π k)
= −1/(2π k) + O(1/k²).
Tiny
compared
with
∫ r sin.

Not
a
campaign
on
Si
itself.
