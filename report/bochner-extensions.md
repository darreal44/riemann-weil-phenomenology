# Extensions of Bochner

**Bochner–Schwartz.**
φ tempered distribution,
positive definite
⇔ φ=ˆμ for a
tempered positive
measure (polynomial
growth). m_Q is
continuous and
bounded (k(t)→C_A
piece + O(1), cos
bounded). Already
a function. B–S
does not add a
measure when
m_Q dips below 0.

**Bochner–Minlos.**
Positive definite
functionals on a
nuclear space.
Wrong category
(we are on ℝ or
on L²(I), not
on 𝒮′ of a
countable Hilbert
scale).

**Schoenberg.**
φ(√t) completely
monotone ⇔ φ is
the characteristic
function of a
euclidean
norm. Not our
m_Q(t).

**Restriction.**
φ≥0 on a subset
K⊂ℝ (the PW
spectrum that
W_L can feed).
That is the only
extension that
could take the
class: prove
m_Q(t)≥0 for t
in the band
that |ˆf|² of
W_L actually
charges, not
for all t≥0.
#57 sampled
the whole line
and also a
cosine family
inside W_L.
The line fails.
The family
does not
fail. A
theorem would
be: the
spectral
support of
W_L avoids
the dip of
m_Q. That is
not Bochner.
It is a
support
statement
about PW_{L/2}.

No extension
restores
(B3) on ℝ.
The live
question is
the support,
not a named
Bochner
variant.
