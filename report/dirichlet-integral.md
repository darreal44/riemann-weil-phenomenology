# Dirichlet integral

∫_0^∞ (sin t)/t dt = π/2 is Si(∞). It is already the limit named in
`sine-integral.md`. The same integral on a finite horizon is Si(2π k).
No new evaluation is required and no Fourier inversion of the rectangle
function is missing from S_k.

The Dirichlet kernel D_N on the circle is a different object (partial
sums of the cosine ONB). Do not fuse them with ∫ D₂ θ.

Not a campaign.
