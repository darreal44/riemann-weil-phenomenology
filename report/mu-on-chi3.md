# Impact of μ on χ₃

Q_window,
no p^k.
H₂ and H₄
only
(#66–#68).

    μ      L        H₂        H₄
    3      log 3    +0.018    (S_lo +0.007 at h=4)
    3.5    log 3.5  −0.012*   +0.0001 (h=4 S_lo)
    4.00            +0.0030   +0.00063
    4.75            +0.00035  +0.00010
    5.00   log 5    +0.00024  −0.00016

*h=2 S_lo
at 3.5, not
λ_min(H₂);
order is
the same.

Trend: the
well sinks
as L grows.
T₃ does
not act
(χ(3)=0).
T₅ does
not act
below 5.
The driver
is A(L):
longer
interval,
more
negative
G_reg
(s₀=3/4)
integrated
against
more of
the
cosine
cross
terms.

At μ=3
raise-h
saved
the
class
in
this
machine.
At μ=5
even
H₄ is
negative:
raise-h
cannot
save a
compression
that is
already
indefinite.
That is
the
impact.
