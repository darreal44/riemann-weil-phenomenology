# Algebra of W_∞

What
one
wants
from
Γ'/Γ
(s₀+it),
cosine
in
t,
kernel
in
y.

The
poles
of
Γ
at
−n
give,
after
the
shift
s₀,
a
geometric
series
in
e^{−2y}:

    ∑_{k≥0} e^{−2(s₀+k) y}
      = e^{−2 s₀ y} / (1−e^{−2y})
      = ½ D₂(y,s₀)

So
D₂
*is*
that
series
(up
to
the
2).
The
identity
D₂ E_C
= 2/(e^{2y}−1)
is
the
same
series
at
s₀=1
(or
the
complement
1−s₀,
because
E_C
= e^{−2(1−s₀)y}
and
s₀ + (1−s₀)=1).
Two
ladders
of
poles:
s₀,s₀+1,…
and
1−s₀,1−s₀+1,…
which
are
exactly
the
two
Γ-factors
of
a
functional
equation
that
pairs
s
with
1−s.

That
is
the
algebra
one
hopes
W_∞
has:

    W_∞
      = a D₂(s₀)
        + b D₂(1−s₀)
        + c / y
        + d δ₀
        (+ maybe
         a
         derivative)

In
Q_nm
the
combination
is
not
written
as
a D₂(s₀)+b D₂(1−s₀).
It
is
D₂(s₀) against
(F0 E_C − θ),
and
E_C
manufactures
D₂(1−s₀)
only
on
the
F0
half,
not
on
the
θ
half.
Asymmetric.

A
symmetric
ansatz
would
be
∫ (α D₂(s₀)+β D₂(1−s₀)) θ
plus
a
constant.
Fitting
α,β
to
A(φ₀)
and
to
c_Γ
is
possible
on
one
χ;
it
is
not
yet
Weil
until
α,β
are
the
Fourier
coefficients
of
Γ'/Γ,
not
a
fit.

The
algebra
is
clear.
The
coefficients
are
not.
