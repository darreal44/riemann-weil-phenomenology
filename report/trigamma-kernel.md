# Trigamma as a kernel

The
cosine
transform
of
ψ(s₀+it)
is
(a
multiple
of)
D₂(s₀).
One
more
t-derivative
on
the
symbol
is
i t
or
a
weight,
not
trigamma.
Trigamma
is
∂/∂s₀
of
ψ:

    ψ^{(1)}(s₀+it)
      = ∂/∂s₀ ψ(s₀+it)

so
its
cosine
transform
is
∂/∂s₀
of
D₂,
i.e.
−2y D₂
up
to
the
normalisation
of
D₂=2 e^{−2 s₀ y}/(1−e^{−2y}).

    ∂_{s₀} D₂ = −2y D₂

A
term
ψ^{(1)}
in
the
symbol
is
therefore
a
kernel
y D₂(y)
in
delay
space:
the
same
ladder,
weighted
by
y.
That
would
appear
if
A
contained
a
derivative
of
Γ
with
respect
to
the
shift
s₀
(it
does
not:
s₀
is
fixed
per
χ).

Known
values:

    ψ^{(1)}(1)
      = ζ(2)=π²/6
    ψ^{(1)}(1/2)
      = π²/2
    ψ^{(1)}(1/4)
      = π² + 8 G
    (G
    Catalan)

None
of
these
is
a
matrix
element
of
Q_nm.
No
code
path
takes
∂_{s₀} A
as
a
form.

Exploring
this
does
not
move
μ*
or
the
cap.
It
only
names
the
y D₂
kernel
one
would
get
by
differentiating
the
family
in
s₀.
