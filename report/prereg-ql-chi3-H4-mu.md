# Preregistration: χ₃ λ_min(H₂) and λ_min(H₄) vs μ

Locked before the run. χ₃ only. Grid
μ = 3.5, 3.75, 4, 4.25, 4.5, 4.75, 5.
h = 2 and 4. Same Q_window as #66/#67
(arch − interior primes). No Neumann,
no tail, no ρ. Not Galerkin as a
positivity proof. A negative H₄ *is*
c_L^*<0 on that 4-plane. Not RH.

#67: H₄(5)<0. #66: H₄(3.5)>0. μ=4 is
the last point with log 2 ≥ L/2
(‖Θ(log 2)‖≤1). H₄ uses θ(log 2),
not the op-norm, so the Θ-jump need
not be the sign change.

**Prediction.** H₂>0 on the whole grid.
H₄>0 at μ=4.75, H₄<0 at μ=5, so
μ* ∈ (4.75, 5]. First negative on the
grid is 5. The hat-unit threshold μ=4
is not the crossing.

**Kill.** H₄(5)≥0, or H₄(3.5)≤0, or
H₂(5)≤0 (would contradict #66/#67).
