# Transfer matrix

Nevanlinna’s
(a b; c d)
is the
transfer
matrix of
the Jacobi
recurrence
from degree
0 to n,
evaluated
at z, then
n→∞ in the
indeterminate
case.

    [P_{n+1}  Q_{n+1}]
    [P_n      Q_n    ]
      = A_n(z) ⋯ A_0(z)
        × initial

Each A_k is
2×2,
det A_k=1,
entries
linear in z
(the three-
term
coefficients
α_k, β_k).
The product
stays in
SL(2). That
is ad−bc=1
at every
finite n,
not only
in the
limit.

On W_{log 3}
the nearest
object we
actually
multiply
is the
block
[H C; C* T]
of Schur,
which is
not SL(2)
and not
a transfer
in z. It
is one
self-adjoint
matrix, not
a family
parametrised
by the
spectral
variable.

Building
the
transfer
means
building
the Jacobi
coefficients
of μ, which
means
building
μ or the
spectrum
of Q̂_L.
Same
missing
input as
P_n and
Q_n.

No
numerical
transfer
to run
from the
current
Q_nm
table
without
choosing
a measure
first.
