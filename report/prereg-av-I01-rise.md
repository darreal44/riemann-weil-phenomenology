# Preregistration: I_{[0,1]} mesh on the convex rise

Locked before the run. Same witness
v=(4,−3,1)/√26, χ₅ μ=16. N=4 mesh
on [0, y_min] misses 0.0053, of
which ~0.0029 lives on [y_min, 1]
(`av-I01-mesh`: the floor after the
min). g is still convex on
[y_min, y_inf]. Extend the same
broken parabolas through that
rise; drop the floor. Chord on
the concave tail [y_inf, 1].
Not Weil. Not RH.

Uniform N=4 nodes on [y_min, y_inf].
On each slab m_i = g''(right end)
(g'''<0 ⇒ g'' decreases to 0 at
y_inf). q_i matches the 1-jet of g
at the left node. a_lo = ½ w g_lo.
True I = ∫ a on [0,1], same shipped
a. Window ±0.003. Finite μ, one v.
Not (∀ L) Q_L≥0.

**Prediction.** Gap tighter than
0.0053. Whether it sits in ±0.003
is measured, not assumed. Q_lo
sign follows. Still one v, not Weil.

**Kill.** Gap ≥ 0.0053 (not tighter
than the well-only mesh), or g'''
changes sign on [0, y_inf].
