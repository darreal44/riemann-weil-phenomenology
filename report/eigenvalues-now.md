# Eigenvalues, now

Three spectra, not one.

    H_n Galerkin of Q̂_L. λ_min monotone ↓, not a take of the operator.
    Θ(y) mid-band cap √2 if the overlap count holds. Finite sections
    saturate √2 (sharpness, not a proof).
    A on the 3-plane of the witness. λ_min of that 3×3 is not I_{[0,1]}.
    I_{[0,1]} is a scalar pairing for one v.

#87–#88 moved that scalar inside WINDOW for one v. They did not move
λ_min(H_n) or ‖Θ‖. Mixing the three spectra is how a Q_lo=+0.005 on one
vector gets misread as RH.

Not a new diagonalisation.
