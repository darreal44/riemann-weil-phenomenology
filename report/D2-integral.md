# The D₂ integral in A

    A_nm = ½ F0 CST
         + ½ ∫_0^L D₂(y) (F0 E_C(y) − θ_nm(y)) dy

On the
constant
mode
F0=2,
θ₀₀(y)=2(L−y)/L,
so

    A₀₀ = CST
        + ½ ∫ D₂ (2 E_C − θ₀₀) dy

χ₃,
L=log 3:
CST=−0.506,
A₀₀=−0.134
(#70).
The
integral
term
is
therefore
+0.372.

That
is
the
piece
that
does
not
match
C_A^{lo}=−0.862:
CST
is
already
near
c_Γ
(−0.566),
the
integral
raises
A
by
0.37,
C_A^{lo}
sits
0.73
below
A.
Either
the
integrand
(E_C,
the
factor
2,
D₂
versus
W_∞)
is
not
Weil,
or
C_A^{lo}
is
not
A(φ₀)
(it
is
a
Bochner
floor
on
ℝ,
not
a
pairing
on
W_L).

E_C
is
the
regularisation
against
the
1/y
in
D₂
(`G_reg`).
Changing
E_C
changes
the
+0.372
without
touching
CST.
That
is
the
next
knob
on
the
integral,
not
more
Gauss
panels
(already
10^{-8}).
