# Rates, collected

    I_{[0,1]} g_lo: O(h²) then O(h³) once leftover=O(h) (#88 cap ratio
    0.123).
    Chebyshev of a_even: coeffs ~10^{−15} after degree 12. a^{(6)} odd:
    no rate (Runge).
    Galerkin λ_min(H_n): monotone ↓, rate not proved.
    Neumann ‖A^k‖≤ρ^k if ρ<1. No rate in L: ρ(L) is not shown to stay
    below 1.
    Weyl zeros: N(T)∼(T/2π)log T, used as a check of harvests, not as a
    rate for Q̂.

These rates do not add. A faster mesh on one v does not move ρ. A
smaller ρ^k does not move I_{[0,1]}.

Not RH.
