# Graph attention networks

GAT is a GNN with learned attention on edges. Same objections as
`gnn.md`: no graph, no labels, no certificate. Attention weights are not
the masses α,β,μ₁,μ₂ of #89.

Not a campaign.
