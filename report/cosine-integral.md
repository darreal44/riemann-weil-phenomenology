# Cosine integral

Ci(x)
= −∫_x^∞ (cos t)/t dt
(or
γ+log x+∫_0^x (cos t−1)/t dt).
It
is
the
companion
of
Si
for
∫ (cos(ω y)/y) dy.

S_k
uses
sin,
not
cos.
Ci
would
appear
in
a
cosine
moment
of
D₂
(the
real
part
of
the
Fourier
table
in
`D2-coeff-decay.md`,
which
decays
slowly
because
Ci(ε)~γ+log ε
at
the
1/y
end).
That
log
is
why
the
cosine
amplitudes
fell
only
from
5.9
to
2.75
by
n=32.
It
does
not
enter
n S_n−m S_m.

Not
a
campaign.
