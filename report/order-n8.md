# Order at N=8

h
halved
from
N=4
to
N=8.

    cap    0.00151 → 0.000186    ratio 0.123
    gap    0.00279 → 0.000405    ratio 0.145

O(h²)
global
predicts
0.25.
O(h³)
global
(leftover
already
O(h))
predicts
0.125.
The
cap
has
hit
the
resolved
rate;
the
gap
is
still
halfway.
That
is
the
bend
named
in
#88
(“rate
set
in”).

No
O(h⁴)
global
(`Oh4.md`).
N=16
should
see
gap
ratio
closer
to
1/8
than
to
1/4
if
the
bend
holds.
