# Hilbert bound, as used

Hilbert
1894:
∑_{n,m≥1}
a_n a_m /(n+m)
≤ π ‖a‖₂².
Hence
‖[1/(n+m)]_{n,m≥1}‖ ≤ π.
The
lab
puts
½π
on
Off_far
because
the
Hankel
piece
of
T
is
1/(2(n+m)).

This
π
is
sharp
on
the
full
ℓ²(ℕ).
It
is
*not*
sharp
on
the
tail
n,m≥N_NEAR:
1/(n+m)≤1/(2 N_NEAR),
so
a
crude
‖Off_far‖≤
(number
of
terms)
is
useless,
but
a
rescaled
Hilbert

    ∑_{n,m≥N}
    a_n a_m /(n+m)
    ≤ π ‖a‖₂²
    still
    holds
    (the
    same
    constant:
    restrict
    a
    to
    vanish
    before
    N).

So
one
cannot
replace
π
by
π/N.
The
1/(4 N_NEAR)
in
Off_far
is
a
separate
HS
or
diagonal
remainder,
not
a
smaller
Hilbert
constant
(`chi5-hankel-not-enough.md`).

A
genuine
gain
would
be
the
norm
of
[1/(n+m)−1/(2 max(n,m))]
or
a
weighted
space
where
the
hats
decay.
That
is
not
π/2
and
is
not
written.

Do
not
touch
π
to
save
ρ_far.
