# Zeros of L, in this repo

They enter Q in
two opposite
ways.

**Zero side.**
Gram
    ∑_γ θ̂(γ)⊗θ̂(γ)
is Q under RH
(or on a window
where all γ in
band are on the
line). Harvest
lists (ζ, χ,
GL₂, Δ, twists,
a few Maass)
feed `scan_gl2`.
Desert: if the
first γ is
beyond the hat
band, Gram is
numerically
0±10⁻¹⁵
(INDEF noise).
That is not a
negative form.

**Prime side.**
Q = A−P never
uses the list.
Zeros would
appear only if
one writes the
closed SOS
T f = (f̂(γ)).
That identity
is the explicit
formula, and
T^*T=Q iff the
γ are real
(Weil). Using
it as a factor
assumes RH.

On W_{log 3}
the first
ordinate of ζ
is ~14, far
above the
Nyquist of
L=log 3. The
window does
not see a
single zero.
Positivity
there is a
statement
about A and
the tower of 2
only. That is
why Schur /
SOS on this L
never mention
a γ.

Maass γ
(first ~17
for maass1)
are in the
same
situation
for μ=3–22:
Gram empty,
prime-side Q
experimental
and INDEF.
