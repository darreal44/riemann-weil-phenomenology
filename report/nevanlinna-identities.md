# Nevanlinna identities

Indeterminate
Hamburger /
Krein problem
on ℝ (or on
an interval
via Cayley):
the set of
μ is

    μ_ω  ↔  ω Pick
    (Im ω(z)≥0
     for Im z>0,
     including
     ω≡const∈ℝ̄)

through

    ∫ dt/(t−z) dμ_ω(t)
      = (a(z)ω(z)+b(z))
        / (c(z)ω(z)+d(z))

where a,b,c,d
are entire
(or analytic
in the cut
plane), built
from the
orthogonal
polynomials
P_n, Q_n of
the moment
sequence,
or from the
Weyl–Titchmarsh
m-function
of the
associated
string /
Jacobi
matrix.

Identities
that come
with that:

    ad−bc = 1
    (Wronskian)
    Im (aω+b)/(cω+d) ≥ 0
    when Im z>0, Im ω≥0
    ω≡∞  →  one
    extremal
    (typically
    atomic)

We do not
have P_n of
G on [−L,L].
Writing
a,b,c,d
would be
the same
work as
diagonalising
the Jacobi
matrix of
the cosine
ONB under
Q̂_L — i.e.
the
spectrum
we said
is not
computed
(`Wlog3-spectrum.md`).

So the
identities
are the
correct
names for
a
parametrisation
we are not
in a
position
to evaluate.
Schur
bypassed
them.
