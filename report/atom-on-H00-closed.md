# Atom on H₀₀, closed form

    θ₀₀(log 2) = 2(L−y)/L
               = 0.73814
    −w₂ θ₀₀     = 0.36178
                 (χ(2)=−1)

    χ₅  A(const)+0.362 = −0.159+0.362 = 0.203
        H₀₀ measured              0.202
    χ₃  A(const)+0.362 = −0.134+0.362 = 0.228
        H₀₀ measured              0.228

The lift is
exact. Larger
than the H₀₁
lift (0.162)
because
θ₀₀(log 2)>
θ₀₁(log 2).
Diagonal gain
0.36, off-
diagonal gain
0.16: the
atom prefers
H₀₀, and
still loses
on det when
H₁₁ is small
(χ₃).
