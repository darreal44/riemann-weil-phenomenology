# Hankel operators, what the lab uses

    Nehari
      ‖Γ‖ = dist(φ, H^∞).
    Hartman
      Γ compact
      ⇔ φ ∈ H^∞ + C(𝕋).
      log(1−z)
      is
      not
      that
      small:
      Hilbert
      is
      not
      compact,
      s_1
      does
      not
      tend
      to
      0
      along
      the
      tail.
    AAK
      s_n(Γ)
      = dist(φ, H^∞ + R_n).
      s_1=π
      is
      Off_far;
      s_n
      for
      n≥2
      do
      not
      replace
      it.

That
is
the
whole
dictionary.
Kronecker
(finite
rank
⇔ rational
symbol)
does
not
apply:
1/j
is
not
the
coefficient
sequence
of
a
rational
function
of
finite
degree
in
a
way
that
kills
s_1.

Not
a
course.
Not
RH.
