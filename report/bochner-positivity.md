# Bochner positivity

A continuous φ is
positive definite
when every
Toeplitz sample
∑_{i,j} φ(t_i−t_j)
c_i ¯c_j ≥ 0, or
equivalently
φ=ˆμ, μ≥0.

For the multiplier
that is the same
as m_Q(t) ≥ 0 for
all t: then
∫ ρ m_Q ≥ 0 for
every ρ≥0, hence
Q(f)≥0 for every
even f with the
normalisation
used in #57.

That is *sufficient*
for Q̂_L≥0, and
strictly stronger
than Q̂_L≥0 on
W_L. W_L only
sees ρ in the
image of the
PW embedding
(supp in frequency
tied to the
interval length
L, plus g(0)=0).

#57: m_Q is not
positive
(α_line<0 on all
four χ). Bochner
positivity fails.
The form on W_L
can still be
positive — the
cosines say it
has not been
killed. Failure
of Bochner
positivity is
not failure of
Q̂_L≥0.

The only way
Bochner becomes
the theorem is
if one proves
inf m_Q ≥ 0.
On W_{log 3}
that is false
for these χ,
so the route
is closed as a
sufficient
test, not as
a disproof.
