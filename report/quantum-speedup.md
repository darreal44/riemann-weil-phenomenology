# Quantum walk speedup

Quadratic speedups for hitting or search (Szegedy, Grover on a graph)
need a marked vertex and a walk unitary. The lab is not searching a
database and has no marked zero.

There is no quantum algorithm here that turns Q_lo=+0.005 on one v into
a class statement, and no speedup of #89 (the pairing is already closed
form).

Not a campaign.
