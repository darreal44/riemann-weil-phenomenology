# Gram matrices on W_{log 3}

Two Grams.

**Zero-side.**
G_γ = (θ̂(γ_i),
θ̂(γ_j)).
Empty on
this L
(first γ
~14 ≫
Nyquist).
Not used
in #58–#65.

**Prime-side
Q_nm.**
The matrix
of Q̂_L in
the cosine
ONB. That
is the
Gram of
the form,
not of
L². Blocks
H, C, T
are pieces
of it.
S_lo is a
bound on
λ_min of
that Gram
as N→∞,
not a
computed
eigenvalue
of a
finite
Gram
beyond
the
section.

Hat Gram
S_N
(`scan_s`)
is the
same form
in another
basis
(tents).
At μ=3,
N=9:
λ_min
=0.044
(χ₅),
0.010
(χ₃) —
same
order
as
S_exact
and
S_lo
after
the
takes.
Different
basis,
same
operator.

Condition
of the
finite
Grams:
H as
above
(κ~10–60),
T
section
κ=O(1).
The
infinite
Gram
is the
operator
Q̂_L;
its
condition
is
λ_max/
c_L^*,
unknown
because
c_L^*
is only
bounded
below.
