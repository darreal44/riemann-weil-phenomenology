# Quadratic forms in play

Weil:

    Q = A − P
    P = ∑_p T_p     (lags log p^k)
    A = archimedean
        (D₂, CST, ψ)

On a window L,
Q_L is this form
restricted to
functions supported
in [−L,L] (or to
W_L ⊂ L²(I)).

Four readings
of the same form:

1. Matrix S_N on
   N hats. Courant:
   λ_min(S_N) ↓ c_L^*
   from above.
   Never a proof
   that c_L^*≥0.

2. Multiplier m_Q
   (#57). Q(f)=
   ∫ ρ m_Q, ρ=|ˆf|².
   Bochner if
   m_Q≥0 on ℝ.
   Fails (α_line<0).
   Stronger than
   Q̂_L≥0.

3. Block
   [H C; C* T]
   (#58–#60).
   Schur of T.
   Takes χ₈, χ₄.
   Majorant of C
   blocks χ₅.

4. Factor
   Q=‖T f‖²
   (SOS desk).
   Spectral T
   exists when
   Q≻0. Arithmetic
   T does not.

Signs already
measured at
L=log 3:

    Q_∞ = A+P−(no primes)
          λ_min < 0
    T₂   indefinite
    Q    λ_min > 0
         on hats
         (razor)

The form that
must be ≥0 for
the class step
is Q̂_L on all
of W_L, not
Q_∞ and not
m_Q on ℝ.
