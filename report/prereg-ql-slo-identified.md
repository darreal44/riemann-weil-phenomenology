# Preregistration: S_lo of identified Q_pk, cap 2

Locked before the run. Re-judge
Neumann Q_pk at χ₃ μ=5 with the
existing cap ‖Θ(y)‖≤2 for y<L/2.
The archimedean gap 0.6 that made
#70 discard S_lo(h≥16) is dead
(`ql-arch-weil`: CST+Gauss is Γ
on W_L). Same majorants, T infinite.
Not a new lemma. Not RH.

**Prediction.** S_lo(2)<0, S_lo(4)<0,
S_lo(16)>0, S_lo(24)>0, λ_H(h)>0
through h=24. Numbers match #70.
This meets the take definition of
`certificates-Wlog3.md` (positive
lower bound, cosine ONB, T infinite).
It is not Weil-positive (one L, not
the class of Weil 1952). Not (∀ L).

**Kill.** S_lo(16)≤0 or λ_H(24)<0.
