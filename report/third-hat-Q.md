# Q split along φ₂

v = (4, −3, 1)/√26
u = (4, −3, 0)/√26     (2-plane piece)
c = 1/√26              (weight of φ₂, 3.8%)

    Q(v) = uᵀHu + 2c uᵀH e₂ + c² H₂₂

    uᵀHu      =  0.00052     already >0
    2c uᵀHe₂  = −0.00418
    c² H₂₂    =  0.00920
    Q(v)      =  0.00554

The raw 2-plane direction
(4, −3, 0) is already a
positive witness, at
5×10^{-4}. φ₂ then adds a
positive diagonal 0.009
and a negative cross
−0.004. Net +0.005, a
factor ten on Q, not the
sign.

Same split of A and P:

              uᵀ·u      2c cross     c² diag     total
    A        −0.898      +0.017       +0.054     −0.828
    P        −0.899      +0.021       +0.045     −0.833

The 2-plane cancellation
A−P on u is 5×10^{-4}.
φ₂'s diagonal is larger
on A than on P (0.054 vs
0.045), which is why it
helps. It is a 4% hat
doing a 10× lift of a
Q that was already
positive.
