# Nevanlinna parametrisation

Setup: moments
s_k=∫ t^k dμ
known for
|k| implicit
in φ on
[−A,A],
problem
indeterminate
(infinitely
many μ).

Then there
is a 2×2
matrix of
analytic
functions
(a b; c d),
ad−bc=1,
such that
every
representing
measure is

    ∫ dμ_ω(t)/(t−z)
      = (a(z)ω(z)+b(z))
        / (c(z)ω(z)+d(z))

for a unique
Pick function
ω (or ω≡const
in the
compactified
reals).
ω≡∞ picks
one
extremal.

Determinate
case: only
ω is
allowed
(or the
formula
collapses
to a
single
Stieltjes
transform).
Then
\(\mathcal{M}\)
is a
point.
We do
not know
which
case we
are in
on
W_{log 3}.

The
parameter
is a
function,
not a
real
number.
Plotting
ω is not
a scan
over t.
