# B₁, χ₃

Section weights Q_{n,n+1} for n=32…46:

    max |Q| = 0.293
    mean = 0.025
    std = 0.200
    2 max = 0.587
    2 rms = 0.404
    ‖B₁‖₂ = 0.396

The weighted-shift bound ‖B₁‖ ≤ 2 max |Q_{n,n+1}| is 0.587 on the
section and is not far from the true 0.396.

Uniform in n: |θ_hat(n,n+1,log 2)| ≤ 0.584 for n=2…399 (and the closed
cap 2/π≈0.637). Hence |w₂ θ| ≤ 0.286 and ‖B₁^{Θ}‖ ≤ 2·0.286 = 0.572 on
the *infinite* codiagonal. That is a genuine operator bound, not a
section.

It does not close Off_far: the other lags still sum harmonically if one
uses |θ|≤2/(πk) band by band. B₁ alone is capped.

Not taken as a replacement of ½π in the certified script. Not RH.
