# s₁ of the true Q tail (χ₃)

Finite section n,m ∈ [32,48):

    ‖Off‖₂ = 0.445
    ‖A‖₂ = 0.097
    qmin = 4.19
    ½π = 1.571

The true off-diagonal block is about 3.5× smaller than the Hilbert
allowance. This is a section, not the essential norm: Hilbert itself is
not compact (Hartman), so a 16×16 block does not prove s₁(Q_tail)<π. It
does show that *this* kernel is not the Hilbert kernel.

Putting 0.445 into Off_far would be a numerical substitute, not a take.
Proving s₁(Q_tail)≤0.6 (say) on ℓ²(n≥32) is the cran that can flip S_lo.
The S_k identity is the symbol; Nehari on *that* symbol is the missing
theorem.

Not taken. Not RH.
