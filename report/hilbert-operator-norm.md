# Operator norm of Hilbert

Standard
continuous
H
(with
the
1/π
in
front)
has
‖H‖_{L²→L²}=1
and
spectrum
{±1}
(or
[−i,i]
in
another
convention).
That
number
is
not
available
as
a
discount
on
∑ a_n a_m/(n+m).

The
bilinear
Hilbert
inequality
is
an
operator
norm
on
ℓ²(ℕ):

    ‖H_Hankel‖
      = π
    (H_Hankel a)_n
      = ∑_m a_m /(n+m).

Sharpness:
a_n = n^{−1/2−ε}
then
ε→0.
The
same
sequence
lives
on
every
tail,
so
‖H_Hankel |_{n≥N}‖
does
not
drop
below
π.

Hence
ρ_far
cannot
be
saved
by
quoting
‖H_cont‖=1.
Different
operators.

Not
a
campaign.
