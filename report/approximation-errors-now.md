# Approximation errors, this line

    Si(2πk) − π/2 = O(1/k) numerically <10^{−3} for k≥2. Negligible.
    ∫ r sin = O(1/k) with a constant not yet proved. This is S_k − π/2
    in practice (~0.02 at k=32).
    Trapezoid on S_k moves the fourth digit (N=8k vs 12k). Not the
    theorem.
    Hilbert ½π vs true |Q_nm| ~0.29 nearby. This is the error that ate
    χ₃.
    g−g_lo O(h³) on [0,1], another v.

The only error that still blocks S_lo(χ₃) on this path is the
replacement of T_far by a Hilbert majorant instead of O((log n)/n) from
n S_n−m S_m. Closing ∫ r sin turns the skeleton into a bound. It does
not by itself recompute S_lo.

Not RH.
